---
import Default from "../layouts/Default.astro";

import data from "../config/site.json";

let title = data.config.mainPageTitle ? data.config.mainPageTitle : data.details.title;
---

<Default title={title}>

</Default>
